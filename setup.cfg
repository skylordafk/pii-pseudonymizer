# Minimal setup.cfg shim so that older pip versions (< 21.3) can perform
# editable installs.  All real metadata lives in pyproject.toml.
[metadata]
name = pii-pseudonymizer

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
